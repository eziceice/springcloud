spring.datasource.url=jdbc:mysql://localhost:3306/springdb?serverTimezone=Australia/Sydney
spring.datasource.username=root
spring.datasource.password=Elementli1.
spring.datasource.tomcat.max-idle=10
spring.datasource.tomcat.max-active=50
spring.datasource.tomcat.max-wait=10000
spring.datasource.tomcat.initial-size=5

mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=com.springboot.pojo
#Redis Config
#配置连接池属性
spring.redis.jedis.pool.min-idle=5
spring.redis.jedis.pool.max-active=10
spring.redis.jedis.pool.max-idle=10
spring.redis.jedis.pool.max-wait=2000
#配置Redis服务器属性
spring.redis.port=6379
spring.redis.host=localhost
#Redis连接超时时间,单位毫秒
spring.redis.timeout=1000

#spring.devtools.livereload.enabled=true
#spring.devtools.livereload.port=35729
##在原来的基础上新增不重启服务的文件夹目录
#spring.devtools.restart.additional-exclude=
##在原来的基础上新增重启服务的文件夹目录
#spring.devtools.restart.additional-paths=
##是否启用自动重启功能
#spring.devtools.restart.enabled=true
##不重启服务的文件夹配置
#spring.devtools.restart.exclude=
management.endpoints.web.exposure.include=*
management.endpoints.web.exposure.exclude=
#never, when-authorized, always
management.endpoint.health.show-details=always
server.port=9003
spring.application.name=mall
eureka.client.serviceUrl.defaultZone=http://localhost:7001/eureka/,http://localhost:7002/eureka/
feign.hystrix.enabled=true
#让Ribbon的负载均衡器不使用从Eureka中获取的注册信息列表-此时需要自己维护一份服务注册列表
#ribbon.eureka.enabled=false
#users.ribbon.listOfServers=http://localhost:9004,http://localhost:9005
